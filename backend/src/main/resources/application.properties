# Server Configuration
server.port=8080
spring.application.name=online-quiz-system

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/quiz_system
spring.data.mongodb.database=quiz_system

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationOnlineQuizSystemVerySecure123456789
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# Logging — configured in logback-spring.xml
logging.config=classpath:logback-spring.xml

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# AI Configuration (ApiFreeLLM)
ai.api.key=apf_k7szsuuf5b8novwzj9ntw7uy
ai.api.url=https://apifreellm.com/api/premium/responses

# Email Configuration (SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:dlyabrawlaacc@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eiylcwhfvlddltzb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# App URL for password reset links
app.base-url=${APP_BASE_URL:http://localhost:3000}
app.mail.from=${MAIL_FROM:dlyabrawlaacc@gmail.com}
ai.api.url=https://apifreellm.com/api/v1/chat
